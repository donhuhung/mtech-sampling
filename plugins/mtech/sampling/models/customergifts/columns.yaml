columns:
    id:
        label: id
        type: number
    customer_id:
        label: customer_id
        relation: customer
        select: name
    gift_id:
        label: gift_id
        relation: gift
        select: gift_name
    location_id:
        label: location_id
        relation: location
        select: location_name
    created_at:
        label: created_at
        type: datetime
    updated_at:
        label: updated_at
        type: datetime
